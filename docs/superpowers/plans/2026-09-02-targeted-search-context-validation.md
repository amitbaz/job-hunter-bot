# Targeted Search and Candidate Context Validation Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Eliminate Brave Search HTTP 422 failures and stop harmless Gemini structured-output variations from forcing candidate-context fallback.

**Architecture:** Keep the existing search backend/fallback and candidate-context cache architecture. Fix Brave at the HTTP request boundary by sending explicit compatibility headers and safe structured error diagnostics; fix candidate parsing by requiring known fields while ignoring unknown extras, without relaxing type/size safeguards.

**Tech Stack:** Python 3.12, requests, dataclasses, pytest, GitHub Actions.

**Spec:** `docs/superpowers/specs/2026-09-02-targeted-search-context-validation-design.md`

## Global Constraints

- Do not change market shares, salary floors, role targeting, Gemini model/quota configuration, Brave monthly budget, or fallback ordering.
- Never log API keys, candidate profile contents, or raw Gemini response bodies.
- Brave attempts must still be recorded before the request.
- Candidate-context fallback remains fail-open when genuinely malformed output is returned.

---

### Task 1: Fix Brave Search request compatibility and diagnostics

**Files:**
- Modify: `src/job_hunter/search_backend.py`
- Modify: `tests/test_search_backend.py`

**Interfaces:**
- `BraveSearchBackend.search(query: str) -> SearchResponse` remains unchanged.
- Add a private safe error-detail formatter used only for logging provider validation errors.

- [ ] Add a failing test asserting Brave sends `Accept-Encoding: gzip` and `Cache-Control: no-cache` alongside the existing authentication/Accept headers.
- [ ] Add a failing test with a 422 JSON response whose validation metadata mentions a header; assert the warning contains only the status/provider/code/detail/validation location+message and does not contain the API key or arbitrary raw response marker.
- [ ] Run `pytest tests/test_search_backend.py -q` and confirm the new tests fail on the current implementation.
- [ ] Implement the two compatibility headers.
- [ ] Catch HTTP errors only long enough to log sanitized structured Brave validation metadata, then re-raise so `FallbackSearchBackend` retains existing DuckDuckGo behavior.
- [ ] Run `pytest tests/test_search_backend.py -q` and require green.

### Task 2: Make candidate-context parsing robust but still validated

**Files:**
- Modify: `src/job_hunter/candidate_context.py`
- Modify: `tests/test_candidate_context.py`
- Modify: `tests/test_first_run_hardening.py` if the existing fallback-log assertion needs the new safe reason.

**Interfaces:**
- `_parse_context(raw: str) -> CandidateContext` keeps the same return type.
- `_parse_preferences(data: object) -> CandidatePreferences` keeps the same return type.
- Fallback `load_error` remains the exception class name for compatibility; the warning gains `reason=<sanitized local validation message>`.

- [ ] Add a failing test where Gemini returns all required fields plus an unknown top-level field and an unknown preference field; assert extraction succeeds and unknown fields are ignored.
- [ ] Add a failing test for a genuinely invalid known field (for example, `technical_skills` as a string); assert fallback occurs and the warning includes the local validation reason but excludes profile/raw-response markers.
- [ ] Run the candidate-context tests and confirm the intended failures.
- [ ] Replace exact-key equality checks with required-known-field subset checks at both top level and preferences level.
- [ ] Log a sanitized `ValueError` reason generated by our parser while preserving `source=fallback_error` and `load_error=ValueError` compatibility.
- [ ] Run candidate-context and first-run-hardening tests and require green.

### Task 3: Regression verification

**Files:** no production changes expected.

- [ ] Run the full CI suite with `pytest -q`.
- [ ] Compare the branch against `main` and confirm only the two intended runtime areas, tests, and docs changed.
- [ ] Confirm no search allocation, Gemini quota/model, salary, or market-policy files changed.
- [ ] Open a PR only after the final branch head is green.